import XCTest
@testable import SwiftPackageWorkshopOne

final class SwiftPackageWorkshopOneTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftPackageWorkshopOne().text, "Hello, World!")
    }
}
